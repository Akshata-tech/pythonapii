fastapi==0.95.0          # FastAPI framework
uvicorn==0.22.0         # ASGI server for serving FastAPI
pydantic==1.10.2        # Data validation and settings management using Python type annotations
httpx==0.24.0           # For making asynchronous HTTP requests (if needed for external API calls)
python-multipart==0.0.6  # For handling file uploads, if your app includes that feature
aiofiles==0.8.0         # For handling file I/O asynchronously, useful for static file serving
